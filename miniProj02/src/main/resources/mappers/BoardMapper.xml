<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosa.hr.board.BoardMapper">
	
	<sql id="search">
        <where>
            <if test="searchKey != null and searchKey != ''">
                btitle like concat('%', #{searchKey}, '%')
            </if>
        </where>
    </sql>
 
	<select id="getList" resultType="org.kosa.hr.entity.BoardVO">
		SELECT B.BNO,B.BTITLE, B.BCONTENT, M.MNAME AS BWRITER , B.BDATE, B.view_count FROM TB_BOARD B INNER JOIN TB_MEMBER M ON B.MID=M.MID  	
		<include refid="search"/>
		order by bno desc limit #{skip}, #{size}
    </select>
    
    <select id="getTotalCount" resultType="int">
		select 
			count(*)   
		from tb_board 
		<include refid="search"/>
    </select>
    
    <select id="view" resultType="org.kosa.hr.entity.BoardVO">
		select 
			b.*, m.mname bwriter  
		from tb_board b inner join tb_member m on b.mid = m.mid  
		where bno = #{bno}
	</select>
	
	<update id="incViewCount" >
		update tb_board set
			 view_count = view_count + 1
		where bno = #{bno}
	</update>





</mapper>